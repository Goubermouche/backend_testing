# GNU Make project makefile autogenerated by Premake

ifndef config
  config=release
endif

ifndef verbose
  SILENT = @
endif

.PHONY: clean prebuild prelink

ifeq ($(config),release)
  TARGETDIR = ../../output/utility/bin/Release
  TARGET = $(TARGETDIR)/utility
  define PREBUILDCMDS
  endef
  define POSTBUILDCMDS
  endef
endif

ifeq ($(config),debug)
  TARGETDIR = ../../output/utility/bin/Debug
  TARGET = $(TARGETDIR)/utility
  define PREBUILDCMDS
  endef
  define POSTBUILDCMDS
  endef
endif

$(TARGET):
	$(PREBUILDCMDS)
	$(POSTBUILDCMDS)
